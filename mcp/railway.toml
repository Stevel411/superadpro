[build]
builder = "NIXPACKS"
nixpacksConfigPath = "mcp/nixpacks.toml"

[deploy]
startCommand = "cd mcp && uvicorn server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
